{
  "name": "edgar-daily-hub",
  "version": "1.0.0",
  "scripts": {
    "start": "concurrently \"uvicorn backend.main:app --reload\" \"npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
