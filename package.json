{
  "name": "edgar-daily-hub",
  "version": "1.0.0",
  "scripts": {
    "start": "concurrently \"cd backend && source venv/bin/activate && uvicorn main:app --reload\" \"npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
